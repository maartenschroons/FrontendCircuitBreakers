export class Vat {
    constructor(public id: number, public nummer: string, public inGebruik: number, public gelinkt: number, public locatie: String, public volume: number, public deksel: boolean, public koelmantel: boolean, public mangat: boolean, public materiaalId: number) { }
}
