export class Process {
    constructor(public procesId: number, public pershoeveelheid: number, public oogst: number, public persingBar: number, public vatId: number, public persMethodeId: number, public druifId: number, public gebruikerId: number){
    
    }
}
